# Bommen – implementatieplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Doel:** Elke speler kan met `bom = (dx, dy)` (dx, dy ∈ −1..1, 3 per potje) een bom op een buurvak leggen: een robot die erop staat of erop stapt is meteen kapot; anders blijft hij liggen als zichtbare mijn.

**Architectuur:** Mijnen komen als eigen lijst `Game.mijnen` naast `Game.schilden`, precies volgens het bestaande patroon (dataclass, `mijn_op`, `_leg_bom` naast `_zet_schild`, nieuwe `Gebeurtenis`-soorten voor het log). De parser krijgt een `±`-plek voor een getal met teken; de editor spiegelt dx voor speler 2 zoals hij nu de schild-x spiegelt. De weergave toont mijnen en knallen via `Cel.mijn` en de bestaande 💥.

**Spec:** `docs/superpowers/specs/2026-09-20-bom-design.md`

**Stack:** Python 3.12, FastAPI, Jinja2, pytest. Pakketbeheer uitsluitend met **uv**: tests draaien met `uv run pytest`.

---

## Bestanden

| Bestand | Wat verandert |
|---|---|
| `app/parser.py` | `Bomb(dx, dy)`, sjabloon `bom=(±,±)`, `HINT_BOM`, `HINT_START` |
| `app/game.py` | `Mijn`, `Game.mijnen`, `Game.knallen`, `Speler.bommen_over`, `_robot_kapot`, `_leg_bom`, mijn-check in `_loop`, meldingen |
| `app/editor.py` | dx spiegelen voor speler 2 |
| `app/ai.py` | Robo stapt niet op een mijn |
| `app/weergave.py` | `Cel.mijn`, knallen → `Cel.raak`, logteksten |
| `app/templates/fragments/sprites.html` | `<symbol id="bom">` |
| `app/templates/fragments/veld.html` | mijn tonen |
| `app/templates/fragments/status.html` | teller 💣 |
| `docs/superpowers/specs/sprites.svg` | dezelfde `<symbol id="bom">` |
| `docs/superpowers/specs/2026-09-20-robotwars-design.md`, `README.md` | taal en regels bijwerken |
| `tests/test_parser.py`, `tests/test_game.py`, `tests/test_editor.py`, `tests/test_ai.py`, `tests/test_weergave.py` | tests |

Alle tests draaien met: `uv run pytest -q` (verwacht: alles groen).

---

### Taak 1: Parser – `bom = (dx, dy)`

**Files:**
- Modify: `app/parser.py`
- Test: `tests/test_parser.py`

- [x] **Stap 1: Schrijf de falende tests**

Voeg onderaan `tests/test_parser.py` toe (na de `expand`-tests):

```python
# ---- bom ----

from app.parser import Bomb, HINT_BOM, HINT_START


def test_bom_met_dx_dy():
    assert parse_line("bom = (-1,1)") == Bomb(-1, 1)
    assert parse_line("bom = ( 1 , 0 )") == Bomb(1, 0)
    assert parse_line("BOM=(0,0)") == Bomb(0, 0)
    assert parse_line("bom = (0, -1)") == Bomb(0, -1)


def test_bom_half_getypt_is_incomplete():
    for tekst in ["b", "bom", "bom =", "bom = (", "bom = (-", "bom = (-1", "bom = (-1,", "bom = (-1, 1"]:
        assert parse_line(tekst) == Incomplete(), tekst


def test_bom_verder_dan_een_vakje_is_invalid():
    assert parse_line("bom = (2, 0)") == Invalid(HINT_BOM)
    assert parse_line("bom = (0, -2)") == Invalid(HINT_BOM)
    assert parse_line("bom = (10, 0)") == Invalid(HINT_BOM)


def test_bom_zonder_of_met_kapotte_getallen_is_invalid():
    assert parse_line("bom = (a, 1)") == Invalid(HINT_BOM)
    assert parse_line("bom = (--1, 0)") == Invalid(HINT_BOM)
    assert parse_line("bom = 1") == Invalid(HINT_BOM)


def test_start_hint_noemt_bom():
    assert parse_line("xyz") == Invalid(HINT_START)
    assert "bom = (...)" in HINT_START


def test_expand_herhaal_met_bom():
    stappen = expand([RepeatStart(2), Bomb(1, 0), RepeatEnd()])
    assert stappen == [Bomb(1, 0), Bomb(1, 0)]
```

Vervang in de bestaande `test_onbekend_begin_is_invalid` de hardgecodeerde hint door de constante:

```python
def test_onbekend_begin_is_invalid():
    r = parse_line("lamp = aan")
    assert isinstance(r, Invalid)
    assert r.hint == HINT_START == "Begin met robot = ..., schild = (...), bom = (...), herhaal ... keer of klaar."
```

- [x] **Stap 2: Draai de tests, ze moeten falen**

Run: `uv run pytest tests/test_parser.py -q`
Verwacht: ImportError (`cannot import name 'Bomb'`).

- [x] **Stap 3: Implementeer in `app/parser.py`**

Voeg na de `Shield`-dataclass toe:

```python
@dataclass(frozen=True)
class Bomb:
    """Bom op een buurvak: dx en dy zijn -1, 0 of 1 ten opzichte van de robot."""
    dx: int
    dy: int
```

Pas de typen en hints aan:

```python
Step = Move | Shoot | Shield | Bomb
```

```python
HINT_BOM = ("Bom heeft twee getallen van -1 tot 1 nodig: bom = (dx, dy), "
            "bijvoorbeeld bom = (1, 0) voor het vak vóór je.")
HINT_START = "Begin met robot = ..., schild = (...), bom = (...), herhaal ... keer of klaar."
BOM_BEREIK = 1   # dx en dy lopen van -BOM_BEREIK t/m BOM_BEREIK
```

Sjablonen: pas het commentaar aan en voeg `bom` toe:

```python
# Sjablonen zonder spaties; '#' staat voor een getal van 1 of 2 cijfers,
# '±' voor een getal van één cijfer met optioneel een minteken ervoor.
_SJABLONEN = (
    "robot=vooruit",
    "robot=achteruit",
    "robot=omhoog",
    "robot=omlaag",
    "robot=schiet",
    "schild=(#,#)",
    "bom=(±,±)",
    "herhaal#keer",
    "klaar",
)
```

In `_past`, voeg een tak voor `±` toe, tussen de `#`-tak en de `else`:

```python
        elif teken == "±":
            start = i
            if compact[i] == "-":
                i += 1
                if i >= len(compact):
                    return False, True, getallen
            if not compact[i].isdigit():
                return False, False, getallen
            i += 1
            getallen.append(int(compact[start:i]))
```

In `_maak_commando`, voeg vóór de `herhaal`-tak toe:

```python
    if sjabloon.startswith("bom"):
        dx, dy = getallen
        if not (-BOM_BEREIK <= dx <= BOM_BEREIK and -BOM_BEREIK <= dy <= BOM_BEREIK):
            return Invalid(HINT_BOM)
        return Bomb(dx, dy)
```

In `_hint`, voeg na de `schild`-tak toe:

```python
    if compact.startswith("bom"):
        return HINT_BOM
```

- [x] **Stap 4: Draai alle parser-tests**

Run: `uv run pytest tests/test_parser.py -q`
Verwacht: alles PASS.

- [x] **Stap 5: Commit**

```bash
git add app/parser.py tests/test_parser.py
git commit -m "Parser: bom = (dx, dy)"
```

---

### Taak 2: Spelmotor – mijnen, knallen en `_robot_kapot`

**Files:**
- Modify: `app/game.py`
- Test: `tests/test_game.py`

- [x] **Stap 1: Schrijf de falende tests**

Voeg onderaan `tests/test_game.py` toe:

```python
# ---- bommen en mijnen ----

from app.game import Mijn, BOMMEN_PER_SPELER, MELD_BOMMEN_OP, MELD_WATER
from app.parser import Bomb


def leg(g, nummer, dx, dy):
    g.spelers[nummer].melding = None
    g.voeg_stappen_toe(nummer, [Bomb(dx, dy)])
    g.tick()
    return g.spelers[nummer].melding


def test_bom_op_leeg_vak_blijft_liggen_als_mijn():
    g = nieuw()
    assert g.spelers[1].bommen_over == BOMMEN_PER_SPELER == 3
    assert leg(g, 1, 1, 1) is None                     # robot op (2,4) → mijn op (3,5)
    assert g.mijn_op(3, 5) == Mijn(3, 5, eigenaar=1)
    assert g.spelers[1].bommen_over == 2
    assert g.knallen == []
    assert g.log[-1] == (1, Gebeurtenis("bom", 1, 3, 5))


def test_mijn_blokkeert_lopen_niet_maar_robot_gaat_kapot():
    g = nieuw()
    g.mijnen.append(Mijn(3, 4, eigenaar=2))
    g.voeg_stappen_toe(1, [Move("vooruit"), Move("vooruit")])
    g.tick()
    s1 = g.spelers[1]
    assert (s1.x, s1.y) == (3, 4)                      # hij kwam er wel
    assert not s1.leeft and s1.respawn_over == RESPAWN_TIKKEN
    assert len(s1.wachtrij) == 0
    assert g.mijn_op(3, 4) is None                     # mijn is weg
    assert g.knallen == [(3, 4)]
    assert soorten(g)[-3:] == ["loop", "mijn_raak", "dood"]
    assert g.log[-2] == (1, Gebeurtenis("mijn_raak", 1, 3, 4, doel=2))
    for _ in range(RESPAWN_TIKKEN):
        g.tick()
    assert s1.leeft and (s1.x, s1.y) == (2, 4)


def test_eigen_mijn_is_ook_gevaarlijk():
    g = nieuw()
    g.mijnen.append(Mijn(3, 4, eigenaar=1))
    g.voeg_stappen_toe(1, [Move("vooruit")])
    g.tick()
    assert not g.spelers[1].leeft
    assert g.log[-2] == (1, Gebeurtenis("mijn_raak", 1, 3, 4, doel=1))


def test_bom_op_tegenstander_is_meteen_kapot():
    g = nieuw()
    g.spelers[1].x, g.spelers[1].y = 8, 3
    g.spelers[2].x, g.spelers[2].y = 9, 4
    assert leg(g, 1, 1, 1) is None
    s2 = g.spelers[2]
    assert not s2.leeft and s2.respawn_over == RESPAWN_TIKKEN
    assert g.mijn_op(9, 4) is None                     # geen mijn achtergebleven
    assert g.knallen == [(9, 4)]
    assert g.spelers[1].bommen_over == 2
    assert soorten(g)[-2:] == ["bom_raak", "dood"]
    assert g.log[-2] == (1, Gebeurtenis("bom_raak", 1, 9, 4, doel=2))


def test_bom_op_eigen_vak_blaast_jezelf_op():
    g = nieuw()
    assert leg(g, 1, 0, 0) is None                     # (2,4) is een startvak, maar er staat een robot
    assert not g.spelers[1].leeft
    assert g.log[-2] == (1, Gebeurtenis("bom_raak", 1, 2, 4, doel=1))


def test_bom_op_tegenstander_op_zijn_startvak_mag():
    g = nieuw()
    g.spelers[1].x, g.spelers[1].y = 11, 3
    assert leg(g, 1, 1, 1) is None                     # (12,4): startvak, robot 2 staat erop
    assert not g.spelers[2].leeft
    assert g.mijn_op(12, 4) is None


def test_bom_op_mijn_laat_beide_knallen_zonder_gewonden():
    g = nieuw()
    g.mijnen.append(Mijn(3, 4, eigenaar=2))
    assert leg(g, 1, 1, 0) is None
    assert g.mijnen == []
    assert g.spelers[1].leeft and g.spelers[2].leeft
    assert g.spelers[1].bommen_over == 2
    assert g.knallen == [(3, 4)]
    assert g.log[-1] == (1, Gebeurtenis("mijn_dubbel", 1, 3, 4, doel=2))


def test_knallen_worden_per_tik_geleegd():
    g = nieuw()
    leg(g, 1, 1, 1)                                    # mijn op (3,5)
    leg(g, 1, 1, 1)                                    # tweede bom erop: beide knallen
    assert g.knallen == [(3, 5)]
    g.tick()
    assert g.knallen == []


def test_bom_mag_op_brug_en_op_andere_helft():
    g = nieuw()
    g.spelers[1].x, g.spelers[1].y = 6, 2
    assert leg(g, 1, 1, 0) is None                     # brug (7,2)
    assert g.mijn_op(7, 2) is not None
    g.spelers[1].x, g.spelers[1].y = 7, 2
    assert leg(g, 1, 1, 0) is None                     # (8,2): helft van speler 2
    assert g.mijn_op(8, 2) is not None


def test_bom_geweigerd_en_niet_verbruikt():
    g = nieuw()
    s1 = g.spelers[1]
    s1.x, s1.y = 1, 1
    assert leg(g, 1, -1, 0) == MELD_BESTAAT_NIET       # (0,1)
    assert leg(g, 1, 0, -1) == MELD_BESTAAT_NIET       # (1,0)
    s1.x, s1.y = 6, 3
    assert leg(g, 1, 1, 0) == MELD_WATER               # (7,3)
    s1.x, s1.y = 2, 3
    assert leg(g, 1, -1, 1) == MELD_BEZET              # (1,4) gebouw
    assert leg(g, 1, 0, 1) == MELD_STARTVAK            # (2,4)
    g.schilden.append(Schild(3, 3, eigenaar=1))
    assert leg(g, 1, 1, 0) == MELD_BEZET               # schild
    assert s1.bommen_over == 3
    assert g.mijnen == []
    assert g.log[-1] == (6, Gebeurtenis("bom_fout", 1, 3, 3, tekst=MELD_BEZET))
    s1.bommen_over = 0
    assert leg(g, 1, 1, 1) == MELD_BOMMEN_OP


def test_kogel_vliegt_over_mijn_heen():
    g = nieuw()
    g.spelers[1].x, g.spelers[1].y = 8, 1
    g.spelers[2].x, g.spelers[2].y = 10, 1
    g.mijnen.append(Mijn(9, 1, eigenaar=2))
    g.voeg_stappen_toe(1, [Shoot()])
    g.tick()
    assert g.spelers[2].robot_levens == 4
    assert g.mijn_op(9, 1) is not None


def test_dode_robot_legt_geen_bom():
    g = nieuw()
    g.spelers[1].robot_levens = 0
    g.spelers[1].respawn_over = RESPAWN_TIKKEN
    g.voeg_stappen_toe(1, [Bomb(1, 0)])
    g.tick()
    assert g.mijnen == [] and g.spelers[1].bommen_over == 3
```

- [x] **Stap 2: Draai de tests, ze moeten falen**

Run: `uv run pytest tests/test_game.py -q`
Verwacht: ImportError (`cannot import name 'Mijn'`).

- [x] **Stap 3: Implementeer in `app/game.py`**

Docstring en import bovenaan:

```python
"""Spelregels van Robot Wars: veld, lopen, schieten, schilden, bommen, respawn, winnen.
```

```python
from .parser import Move, Shoot, Shield, Bomb, Step
```

Constanten (na `SCHILDEN_PER_SPELER`):

```python
BOMMEN_PER_SPELER = 3
```

Meldingen (na `MELD_BEZET`):

```python
MELD_BOMMEN_OP = "Je bommen zijn op."
MELD_WATER = "Daar is water."
```

Dataclass na `Schild`:

```python
@dataclass
class Mijn:
    """Een bom die is blijven liggen: gaat af zodra een robot erop stapt (van wie ook)."""
    x: int
    y: int
    eigenaar: int
```

Commentaar bij `Gebeurtenis.soort` uitbreiden:

```python
    soort: str                   # loop, geblokkeerd, raak_schild, raak_robot, raak_gebouw, mis,
                                 # dood, terug, schild, schild_fout, bom, bom_fout, bom_raak,
                                 # mijn_raak, mijn_dubbel, win
```

`Speler` krijgt na `schilden_over`:

```python
    bommen_over: int = BOMMEN_PER_SPELER
```

`Game.__init__` na `self.schoten = []`:

```python
        self.mijnen: list[Mijn] = []
        self.knallen: list[tuple[int, int]] = []   # explosies van de laatste tik (bom/mijn), voor de 💥
```

Na `schild_op`:

```python
    def mijn_op(self, x: int, y: int) -> Mijn | None:
        return next((m for m in self.mijnen if (m.x, m.y) == (x, y)), None)
```

`is_vrij` blijft ongewijzigd (een mijn blokkeert niets; wie erop stapt, gaat kapot).

In `tick()`, na `self.schoten = []`:

```python
        self.knallen = []
```

`_voer_uit`:

```python
    def _voer_uit(self, speler: Speler, stap: Step) -> None:
        if isinstance(stap, Move):
            self._loop(speler, stap.richting)
        elif isinstance(stap, Shoot):
            self._schiet(speler)
        elif isinstance(stap, Shield):
            self._zet_schild(speler, stap.x, stap.y)
        elif isinstance(stap, Bomb):
            self._leg_bom(speler, stap.dx, stap.dy)
```

Nieuwe helper, direct na `_respawn`:

```python
    def _robot_kapot(self, robot: Speler, x: int, y: int) -> None:
        """Robot sneuvelt op (x, y): hartjes op 0, wachtrij leeg, terug na RESPAWN_TIKKEN."""
        robot.robot_levens = 0
        robot.respawn_over = RESPAWN_TIKKEN
        robot.wachtrij.clear()
        self._meld("dood", robot.nummer, x, y)
```

`_loop`: vervang de `if self.is_vrij(*doel):`-tak door:

```python
        if self.is_vrij(*doel):
            speler.x, speler.y = doel
            self._meld("loop", speler.nummer, speler.x, speler.y, tekst=richting)
            mijn = self.mijn_op(*doel)
            if mijn is not None:
                self.mijnen.remove(mijn)
                self.knallen.append(doel)
                self._meld("mijn_raak", speler.nummer, *doel, doel=mijn.eigenaar)
                self._robot_kapot(speler, *doel)
        else:
            self._meld("geblokkeerd", speler.nummer, tekst=richting)
```

`_schiet`: vervang in de robot-tak

```python
                if robot.robot_levens == 0:
                    robot.respawn_over = RESPAWN_TIKKEN
                    robot.wachtrij.clear()
                    self._meld("dood", robot.nummer, x, y)
```

door

```python
                if robot.robot_levens == 0:
                    self._robot_kapot(robot, x, y)
```

Nieuwe methode na `_zet_schild`:

```python
    def _leg_bom(self, speler: Speler, dx: int, dy: int) -> None:
        """Bom op een buurvak (dx, dy al in echte veldrichting). Staat er een robot: meteen
        kapot. Ligt er een mijn: beide weg. Anders blijft de bom liggen als mijn."""
        x, y = speler.x + dx, speler.y + dy
        if speler.bommen_over == 0:
            speler.melding = MELD_BOMMEN_OP
        elif not in_veld(x, y):
            speler.melding = MELD_BESTAAT_NIET
        elif is_water(x, y):
            speler.melding = MELD_WATER
        elif self.gebouw_op(x, y) is not None or self.schild_op(x, y) is not None:
            speler.melding = MELD_BEZET
        elif (x, y) in START.values() and self.robot_op(x, y) is None:
            speler.melding = MELD_STARTVAK   # anders zou de mijn blijven liggen en kan niemand terugkomen
        else:
            speler.bommen_over -= 1
            speler.melding = None
            robot = self.robot_op(x, y)
            mijn = self.mijn_op(x, y)
            if robot is not None:
                self.knallen.append((x, y))
                self._meld("bom_raak", speler.nummer, x, y, doel=robot.nummer)
                self._robot_kapot(robot, x, y)
            elif mijn is not None:
                self.mijnen.remove(mijn)
                self.knallen.append((x, y))
                self._meld("mijn_dubbel", speler.nummer, x, y, doel=mijn.eigenaar)
            else:
                self.mijnen.append(Mijn(x, y, speler.nummer))
                self._meld("bom", speler.nummer, x, y)
            return
        self._meld("bom_fout", speler.nummer, x, y, tekst=speler.melding)
```

- [x] **Stap 4: Draai alle game-tests**

Run: `uv run pytest tests/test_game.py -q`
Verwacht: alles PASS (ook de bestaande dood/respawn-tests, die nu via `_robot_kapot` lopen).

- [x] **Stap 5: Commit**

```bash
git add app/game.py tests/test_game.py
git commit -m "Spelmotor: bommen en mijnen"
```

---

### Taak 3: Editor – dx spiegelen voor speler 2

**Files:**
- Modify: `app/editor.py`
- Test: `tests/test_editor.py`

- [x] **Stap 1: Schrijf de falende test**

Voeg na `test_schild_van_speler_2_wordt_gespiegeld_naar_veldcoordinaten` toe:

```python
def test_bom_dx_is_vooruit_dus_gespiegeld_voor_speler_2():
    from app.parser import Bomb
    g, e = nieuw()
    e.verwerk(g, 1, "bom = (1, -1)")
    assert list(g.spelers[1].wachtrij) == [Bomb(1, -1)]
    e2 = Editor()
    e2.verwerk(g, 2, "bom = (1, -1)")                  # vooruit is voor speler 2 kolom -1
    assert list(g.spelers[2].wachtrij) == [Bomb(-1, -1)]
    e2.verwerk(g, 2, "bom = (0, 1)")
    assert list(g.spelers[2].wachtrij)[-1] == Bomb(0, 1)
    assert e2.regels[-1].tekst == "bom = (0, 1)"       # de getypte regel blijft zoals getypt
```

- [x] **Stap 2: Draai de test, hij moet falen**

Run: `uv run pytest tests/test_editor.py -q -k bom`
Verwacht: FAIL, `[Bomb(dx=1, dy=-1)] != [Bomb(dx=-1, dy=-1)]`.

- [x] **Stap 3: Implementeer in `app/editor.py`**

Imports:

```python
from .game import Game, MAX_WACHTRIJ, MELD_DRUK, RICHTING, eigen_kolom
from .parser import (Bomb, Command, Incomplete, Invalid, RepeatEnd, RepeatStart, Shield,
                     expand, parse_line)
```

Direct na het `Shield`-spiegelblok in `verwerk`:

```python
        if isinstance(r, Bomb):
            # dx = 1 is "vooruit"; voor speler 2 is dat op het veld kolom -1
            r = Bomb(r.dx * RICHTING[nummer], r.dy)
```

Werk het commentaar `# Move / Shoot / Shield` bij naar `# Move / Shoot / Shield / Bomb`.

- [x] **Stap 4: Draai de editor-tests**

Run: `uv run pytest tests/test_editor.py -q`
Verwacht: alles PASS.

- [x] **Stap 5: Commit**

```bash
git add app/editor.py tests/test_editor.py
git commit -m "Editor: bom-dx spiegelen voor speler 2"
```

---

### Taak 4: Robo stapt niet op een mijn

**Files:**
- Modify: `app/ai.py`
- Test: `tests/test_ai.py`

- [x] **Stap 1: Schrijf de falende tests**

Voeg onderaan `tests/test_ai.py` toe:

```python
from app.game import Mijn


def test_robo_stapt_niet_op_een_mijn_maar_kiest_de_andere_brug():
    g = nieuw()
    g.spelers[1].x, g.spelers[1].y = 2, 1        # vijand uit de weg
    g.spelers[2].x, g.spelers[2].y = 12, 4
    g.mijnen.append(Mijn(12, 3, eigenaar=1))     # op weg naar de onderste brug
    assert kies_stap(g, 2) == Move("omhoog")
    g.spelers[2].y = 2                           # op de rij van de brug
    g.mijnen[:] = [Mijn(11, 2, eigenaar=1)]      # mijn vóór hem
    assert kies_stap(g, 2) == Move("omhoog")     # dan maar naar de brug op y=6


def test_robo_wacht_voor_mijn_op_de_brug_zelf():
    g = nieuw()
    g.spelers[1].x, g.spelers[1].y = 2, 1
    g.spelers[2].x, g.spelers[2].y = 7, 2        # op de brug, mijn op (6,2)
    g.mijnen.append(Mijn(6, 2, eigenaar=1))
    assert kies_stap(g, 2) is None               # geblokkeerd: even wachten, niet erop stappen


def test_robo_schiet_gewoon_over_een_mijn_heen():
    g = nieuw()
    g.spelers[1].x, g.spelers[1].y = 8, 4
    g.spelers[2].x, g.spelers[2].y = 11, 4
    g.mijnen.append(Mijn(9, 4, eigenaar=1))
    assert kies_stap(g, 2) == Shoot()
```

- [x] **Stap 2: Draai de tests, ze moeten falen**

Run: `uv run pytest tests/test_ai.py -q -k mijn`
Verwacht: de eerste twee FAIL (Robo geeft `Move("omlaag")`/`Move("vooruit")`), de derde PASS.

- [x] **Stap 3: Implementeer in `app/ai.py`**

`_stap_of_schot`:

```python
def _stap_of_schot(game: Game, ik: Speler, richting: str) -> Step | None:
    """Zet een stap als het vak vrij is en er geen mijn ligt. Staat er vooruit een schild
    in de weg, schiet er dan op. Anders None (even wachten)."""
    x, y = _doel(ik, richting)
    if game.is_vrij(x, y) and game.mijn_op(x, y) is None:
        return Move(richting)
    if richting == "vooruit" and game.schild_op(x, y) is not None:
        return Shoot()
    return None
```

In `_loop_stap`, vervang het `if not over:`-blok door:

```python
    if not over:
        brug = min(BRUG_RIJEN, key=lambda rij: (abs(rij - ik.y), rij))
        andere = BRUG_RIJEN[1] if brug == BRUG_RIJEN[0] else BRUG_RIJEN[0]
        if ik.y == brug:
            stap = _stap_of_schot(game, ik, "vooruit")
            if stap is None and game.mijn_op(*_doel(ik, "vooruit")) is not None:
                return _stap_of_schot(game, ik, _verticaal(ik.y, andere))   # mijn op de weg: andere brug
            return stap
        stap = _stap_of_schot(game, ik, _verticaal(ik.y, brug))
        if stap is not None:
            return stap
        return _stap_of_schot(game, ik, _verticaal(ik.y, andere))
```

- [x] **Stap 4: Draai de AI-tests**

Run: `uv run pytest tests/test_ai.py -q`
Verwacht: alles PASS.

- [x] **Stap 5: Commit**

```bash
git add app/ai.py tests/test_ai.py
git commit -m "Robo: loopt om mijnen heen"
```

---

### Taak 5: Weergave – mijnen, knallen en logteksten

**Files:**
- Modify: `app/weergave.py`
- Test: `tests/test_weergave.py`

- [x] **Stap 1: Schrijf de falende tests**

Vervang bovenaan `tests/test_weergave.py` de import

```python
from app.game import Game, Schild, Gebeurtenis, MELD_HELFT
```

door

```python
from app.game import Game, Schild, Gebeurtenis, MELD_HELFT, MELD_BEZET
```

Voeg onderaan toe:

```python
# ---- bommen ----

from app.game import Mijn, MELD_WATER


def test_matrix_toont_mijn_en_knal():
    g = Game("g", "A", "B")
    g.mijnen.append(Mijn(5, 3, eigenaar=2))
    g.knallen.append((9, 6))
    rijen = veld_matrix(g, ik=1)
    assert rijen[4][4].mijn == Mijn(5, 3, eigenaar=2)     # (5,3): y=3 is gridrij 5
    assert rijen[4][3].mijn is None
    knal = rijen[1][8]                                    # (9,6)
    assert knal.raak and knal.knal_vertraging == 0 and not knal.spoor
    rijen2 = veld_matrix(g, ik=2)                         # gespiegeld: x=5 staat op schermkolom 9
    assert rijen2[4][8].mijn is not None


def test_log_regels_bommen():
    g = Game("g", "Wessel", "Robo")
    for tik, e in [
        (1, Gebeurtenis("bom", 1, 3, 5)),
        (2, Gebeurtenis("bom_fout", 1, 7, 3, tekst=MELD_WATER)),
        (3, Gebeurtenis("bom_raak", 1, 9, 4, doel=2)),
        (4, Gebeurtenis("bom_raak", 2, 8, 4, doel=1)),
        (5, Gebeurtenis("bom_raak", 1, 2, 4, doel=1)),
        (6, Gebeurtenis("mijn_raak", 2, 9, 4, doel=1)),
        (7, Gebeurtenis("mijn_raak", 1, 4, 4, doel=1)),
        (8, Gebeurtenis("mijn_dubbel", 2, 7, 2, doel=1)),
        (9, Gebeurtenis("bom", 2, 10, 3)),
        (10, Gebeurtenis("bom_fout", 2, 13, 4, tekst=MELD_BEZET)),
    ]:
        g.log.append((tik, e))
    oudste_eerst_1 = [r["tekst"] for r in log_regels(g, 1, aantal=99)][::-1]
    assert oudste_eerst_1 == [
        "Jij legt een bom op (3, 5)",
        f"Bom geweigerd: {MELD_WATER}",
        "💥 Jouw bom raakt Robo!",
        "💥 De bom van Robo raakt jou!",
        "💥 Je legt een bom op jezelf!",
        "💥 Robo stapt op een mijn!",
        "💥 Je stapt op een mijn!",
        "Twee mijnen knallen op (7, 2)",
        "Robo legt een bom op (10, 3)",
        "Robo probeert een bom, maar dat mag niet",
    ]
    oudste_eerst_2 = [r["tekst"] for r in log_regels(g, 2, aantal=99)][::-1]
    assert oudste_eerst_2[2:6] == [
        "💥 De bom van Wessel raakt jou!",
        "💥 Jouw bom raakt Wessel!",
        "💥 Wessel legt een bom op zichzelf!",
        "💥 Je stapt op een mijn!",
    ]
    assert oudste_eerst_2[8] == "Jij legt een bom op (4, 3)"   # speler 2 ziet kolommen gespiegeld
```

- [x] **Stap 2: Draai de tests, ze moeten falen**

Run: `uv run pytest tests/test_weergave.py -q -k "mijn or bommen"`
Verwacht: FAIL (`Cel` heeft geen `mijn`; logtekst geeft de ruwe soort terug).

- [x] **Stap 3: Implementeer in `app/weergave.py`**

Import:

```python
from .game import (BREEDTE, BRUG_RIJEN, GEBOUW_LEVENS, HOOGTE, RESPAWN_TIKKEN, RIVIER_X,
                   ROBOT_LEVENS, Game, Gebeurtenis, Mijn, Schild, Speler, eigen_kolom)
```

`Cel` krijgt na `schild`:

```python
    mijn: Mijn | None = None
```

In `veld_matrix`, de `Cel(...)`-aanroep en daarna de knal-check (vóór de `for schot`-lus):

```python
            cel = Cel(x, y, soort, robot=game.robot_op(x, y),
                      gebouw=game.gebouw_op(x, y), schild=game.schild_op(x, y),
                      mijn=game.mijn_op(x, y))
            if (x, y) in game.knallen:          # bom of mijn ging hier af: 💥 zonder kogelbaan
                cel.raak = True
```

In `log_tekst`, vóór de `win`-tak:

```python
    if e.soort == "bom":
        return f"{wie} legt een bom op ({schermkolom(e.x, ik)}, {e.y})"
    if e.soort == "bom_fout":
        return f"Bom geweigerd: {e.tekst}" if mij else f"{wie} probeert een bom, maar dat mag niet"
    if e.soort == "bom_raak":
        if e.doel == e.speler:
            return "💥 Je legt een bom op jezelf!" if mij else f"💥 {wie} legt een bom op zichzelf!"
        return f"💥 De bom van {wie} raakt jou!" if doel_mij else f"💥 Jouw bom raakt {doel_naam}!"
    if e.soort == "mijn_raak":
        return "💥 Je stapt op een mijn!" if mij else f"💥 {wie} stapt op een mijn!"
    if e.soort == "mijn_dubbel":
        return f"Twee mijnen knallen op ({schermkolom(e.x, ik)}, {e.y})"
```

(Na `bom_raak` en `mijn_raak` volgt in het spel altijd de bestaande `dood`-regel: "💥 Je robot is kapot! Hij komt terug over 3 seconden".)

- [x] **Stap 4: Draai de weergave-tests**

Run: `uv run pytest tests/test_weergave.py -q`
Verwacht: alles PASS.

- [x] **Stap 5: Commit**

```bash
git add app/weergave.py tests/test_weergave.py
git commit -m "Weergave: mijnen, knallen en bom-logregels"
```

---

### Taak 6: Sprite, veld en statuskaart

**Files:**
- Modify: `app/templates/fragments/sprites.html`
- Modify: `app/templates/fragments/veld.html`
- Modify: `app/templates/fragments/status.html`
- Modify: `docs/superpowers/specs/sprites.svg`

Hier zijn geen unit-tests (de templates worden handmatig in de browser bekeken); de bestaande `tests/test_main.py` rendert de spelpagina wel, dus een kapotte template valt daar op.

- [x] **Stap 1: Voeg de bom-sprite toe aan `app/templates/fragments/sprites.html`**

Direct vóór `<!-- KOGEL -->`:

```html
    <!-- BOM: zwarte bol met lont en vonk, ringetje in de teamkleur van de eigenaar -->
    <radialGradient id="bomG" cx=".35" cy=".3" r=".75"><stop offset="0" stop-color="#6b7080"/><stop offset=".55" stop-color="#23262f"/><stop offset="1" stop-color="#0b0c10"/></radialGradient>
    <symbol id="bom" viewBox="0 0 100 100">
      <ellipse cx="50" cy="92" rx="26" ry="4" fill="rgba(0,0,0,.45)"/>
      <circle cx="50" cy="60" r="34" fill="none" stroke="var(--kleur)" stroke-width="3" opacity=".8" filter="url(#glow)"/>
      <circle cx="50" cy="60" r="27" fill="url(#bomG)" filter="url(#schaduw)"/>
      <ellipse cx="40" cy="48" rx="8" ry="5" fill="#fff" opacity=".35"/>
      <rect x="44" y="27" width="12" height="9" rx="2" fill="#8d93a3"/>
      <path d="M50 28 C50 18, 62 18, 66 10" stroke="#c9a66b" stroke-width="3.5" fill="none" stroke-linecap="round"/>
      <circle cx="67" cy="9" r="6" fill="url(#gloed)"><animate attributeName="r" values="4;8;4" dur=".4s" repeatCount="indefinite"/></circle>
    </symbol>

```

- [x] **Stap 2: Zet dezelfde symbol in `docs/superpowers/specs/sprites.svg`**

Op dezelfde plek (vóór `<!-- KOGEL -->`), exact dezelfde regels als in stap 1.

- [x] **Stap 3: Toon de mijn in `app/templates/fragments/veld.html`**

Na de `{% elif cel.schild %}`-tak (en vóór `{% elif cel.robot %}`):

```html
    {% elif cel.mijn %}
      <svg class="{{ cel.mijn.eigenaar|kleur }}"><use href="#bom"/></svg>
```

(Een mijn en een levende robot staan nooit samen op een vak: wie erop stapt, gaat kapot en `robot_op` negeert dode robots.)

- [x] **Stap 4: Teller in `app/templates/fragments/status.html`**

Na de regel met `🛡️ nog {{ p.schilden_over }}`:

```html
      <span class="stat">💣 nog {{ p.bommen_over }}</span>
```

- [x] **Stap 5: Draai alle tests**

Run: `uv run pytest -q`
Verwacht: alles PASS.

- [x] **Stap 6: Bekijk het in de browser**

Run: `uv run uvicorn app.main:app --port 8000`
Open http://localhost:8000, speel tegen de computer en typ:

```
bom = (1, 1)
robot = vooruit
robot = omhoog
```

Verwacht: na de eerste regel ligt een zwarte bom met blauw ringetje op (3,5) en staat "💣 nog 2" in je statuskaart; na `omhoog` stapt je robot erop, er verschijnt een 💥, het log meldt "💥 Je stapt op een mijn!" en "💥 Je robot is kapot! …", en de robot komt na 3 seconden terug. Typ daarna `bom = (2, 0)` en zie de rode `!` met de bomhint. Stop de server met Ctrl+C.

- [x] **Stap 7: Commit**

```bash
git add app/templates/fragments/sprites.html app/templates/fragments/veld.html app/templates/fragments/status.html docs/superpowers/specs/sprites.svg
git commit -m "Weergave: bom-sprite, mijn op het veld en bommenteller"
```

---

### Taak 7: Documentatie bijwerken

**Files:**
- Modify: `docs/superpowers/specs/2026-09-20-robotwars-design.md`
- Modify: `README.md`

- [x] **Stap 1: Hoofdontwerp – spelregels**

In `docs/superpowers/specs/2026-09-20-robotwars-design.md`, voeg na de paragraaf "### Schilden" (vóór "### Winnen") toe:

```markdown
### Bommen

`bom = (dx, dy)` legt een bom op een buurvak van de robot: dx en dy zijn elk
−1, 0 of 1, met dx = 1 "vooruit" (richting tegenstander) en dy = 1 omhoog.
Elke speler heeft er 3 per potje. Staat er een robot (ook jijzelf bij
`(0, 0)`), dan is die meteen kapot. Ligt er al een mijn, dan knallen beide en
raakt niemand gewond. Anders blijft de bom liggen als **mijn**, zichtbaar voor
allebei: een robot die erop stapt (van wie ook) is meteen kapot. Kogels
vliegen over mijnen heen. Niet toegestaan (foutmelding, bom niet verbruikt):
buiten het veld, water, gebouw, schild, startvak. Robo legt geen bommen en
loopt om mijnen heen. Volledig ontwerp: `2026-09-20-bom-design.md`.
```

- [x] **Stap 2: Hoofdontwerp – de taal**

In hoofdstuk 4, in het codeblok met voorbeelden, voeg na `schild = (4, 2)` toe:

```
bom = (1, 0)
```

In de parser-beschrijving (hoofdstuk 4, "Parser"): vervang

```
    `Shield(x, y)`, `RepeatStart(n)`, `RepeatEnd()`);
```

door

```
    `Shield(x, y)`, `Bomb(dx, dy)`, `RepeatStart(n)`, `RepeatEnd()`);
```

en `lijst stappen (`Move`, `Shoot`, `Shield`)` door `lijst stappen (`Move`, `Shoot`, `Shield`, `Bomb`)`. Voeg bij de voorbeeldhints toe:

```markdown
- `Bom heeft twee getallen van -1 tot 1 nodig: bom = (dx, dy), bijvoorbeeld bom = (1, 0) voor het vak vóór je.`
```

Werk in hoofdstuk 9 de regel van `game.py` bij naar: `spelregels (veld, lopen, schieten, schilden, bommen, respawn, winnen)`.

- [x] **Stap 3: README**

In het codeblok onder "## Spelen", na de `schild`-regel:

```
bom = (1, 0)         (dx, dy van -1 tot 1 rondom je robot; 3 per potje; robot erop = kapot)
```

En in de bestandenlijst de regel van `app/game.py`: `de spelregels (veld, lopen, schieten, schilden, bommen, winnen)`.

- [x] **Stap 4: Commit**

```bash
git add docs/superpowers/specs/2026-09-20-robotwars-design.md README.md
git commit -m "Docs: bommen in ontwerp en README"
```

---

### Taak 8: Afronden

- [x] **Stap 1: Alle tests**

Run: `uv run pytest -q`
Verwacht: alles PASS.

- [x] **Stap 2: Vink de taken in dit plan af en commit het plan**

```bash
git add docs/superpowers/plans/2026-09-20-bommen.md
git commit -m "Plan: bommen afgerond"
```

Daarna: `superpowers:finishing-a-development-branch` (push naar master = auto-deploy naar robot.schut.me).
